name: modular_posts
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  dio: ^3.0.9 # REST APIs requests
  flutter_modular: ^1.2.7+1 # Routing and state management
  mobx: ^1.2.1+1 # MobX State management code
  flutter_mobx: ^1.1.0+1 # MobX integration to Flutter
  expandable: ^4.1.4 # Expandable cards


dev_dependencies:
  flutter_test:
    sdk: flutter
  mobx_codegen: ^1.1.0+1 # Support for annotation code like @observable, @computed, @action
  build_runner: ^1.10.0 # Provides a concrete way of generating files using Dart code

flutter:
  uses-material-design: true

  assets:
    - assets/ # Doesn't work
    - assets/img/profile.png
  #   - images/a_dot_ham.jpeg

  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700